<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/son">

    <!-- İçerik ScrollView -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp">

            <!-- Başlık -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Sepetim"
                android:textColor="@android:color/white"
                android:textSize="28sp"
                android:textStyle="bold"
                android:layout_marginBottom="16dp" />

            <!-- Boş sepet mesajı -->
            <TextView
                android:id="@+id/txtSepetBos"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Sepetiniz bos.\nCicekler sekmesinden urun ekleyin"
                android:textColor="#CCCCCC"
                android:textSize="16sp"
                android:gravity="center"
                android:padding="40dp"
                android:visibility="visible" />

            <!-- Sepet listesi -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recyclerViewSepet"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:nestedScrollingEnabled="false"
                android:visibility="gone" />

            <!-- Toplam fiyat -->
            <TextView
                android:id="@+id/txtToplamFiyat"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Toplam: 0 TL"
                android:textColor="#FFD700"
                android:textSize="20sp"
                android:textStyle="bold"
                android:gravity="end"
                android:layout_marginTop="12dp"
                android:layout_marginBottom="8dp" />

            <!-- Sepeti boşalt butonu -->
            <Button
                android:id="@+id/btnSepetiBosalt"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:text="Sepeti Boşalt"
                android:textAllCaps="false"
                android:textColor="#FFFFFF"
                android:backgroundTint="@color/peony"
                android:layout_marginBottom="12dp" />

            <!-- Buket oluştur butonu -->
            <Button
                android:id="@+id/btnBuketOlustur"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:text="AI ile Buket Aciklamasi Olustur"
                android:textAllCaps="false"
                android:textColor="#FFFFFF"
                android:textSize="15sp"
                android:textStyle="bold"
                android:backgroundTint="@color/sage"/>


"
            <!-- Prompt alanı (başta gizli) -->
            <LinearLayout
                android:id="@+id/layoutBuketPrompt"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:visibility="gone"
                android:background="#33FFFFFF"
                android:padding="16dp"
                android:layout_marginBottom="16dp">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Buketinizi nasil hayal ediyorsunuz?"
                    android:textColor="@android:color/white"
                    android:textSize="14sp"
                    android:textStyle="bold"
                    android:layout_marginBottom="8dp" />

                <EditText
                    android:id="@+id/editPrompt"
                    android:layout_width="match_parent"
                    android:layout_height="100dp"
                    android:gravity="top|start"
                    android:hint="Örn: Romantik bir sevgililer günü buketi, çiçekler mor ve pembe tonlarda olsun..."
                    android:textColorHint="#AAAAAA"
                    android:textColor="@android:color/white"
                    android:textSize="14sp"
                    android:padding="12dp"
                    android:background="#55000000"
                    android:layout_marginBottom="12dp"
                    android:inputType="textMultiLine"
                    android:lines="3" />

                <Button
                    android:id="@+id/btnPromptGonder"
                    android:layout_width="match_parent"
                    android:layout_height="48dp"
                    android:text="Buket Aciklamasi Olustur"
                    android:textAllCaps="false"
                    android:textColor="#FFFFFF"
                    android:backgroundTint="#6A0DAD" />

            </LinearLayout>

            <!-- Yükleniyor -->
            <ProgressBar
                android:id="@+id/progressBar"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:visibility="gone"
                android:layout_marginBottom="16dp" />

            <!-- AI Sonucu -->
            <androidx.cardview.widget.CardView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="20dp"
                app:cardCornerRadius="16dp"
                app:cardBackgroundColor="@color/peony"
                app:cardElevation="6dp">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:padding="16dp">

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="AI Buket Aciklamasi"
                        android:textColor="@color/white"
                        android:textSize="16sp"
                        android:textStyle="bold"
                        android:layout_marginBottom="8dp" />

                    <TextView
                        android:id="@+id/txtBuketSonuc"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:textColor="#E0E0E0"
                        android:textSize="14sp"
                        android:lineSpacingMultiplier="1.5" />

                </LinearLayout>
            </androidx.cardview.widget.CardView>

        </LinearLayout>
    </ScrollView>

    <!-- Alt Tab -->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tabLayout"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:background="@drawable/shapes2"
        app:tabTextColor="#BDBDBD"
        app:tabSelectedTextColor="#FFD700"
        app:tabIndicatorGravity="top"
        app:tabIndicatorColor="#FFD700"
        app:tabMode="fixed"
        android:elevation="15dp"/>

</LinearLayout>
